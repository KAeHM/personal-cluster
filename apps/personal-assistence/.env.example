# Docker Compose — copie para .env na raiz do projeto
EVOLUTION_API_KEY=dev-secret-key-change-me

# URL do webhook Next.js (usada pelo setup-evolution.sh)
WEBHOOK_URL=http://host.docker.internal:3000/api/webhooks/whatsapp

# Versão WhatsApp Web — WhatsApp Desktop → Configurações → Ajuda (rodapé)
WHATSAPP_WEB_VERSION=2.3000.1040323813
