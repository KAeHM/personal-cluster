import { describe, expect, it } from "vitest";

/**
 * Testes de integração com Supabase Cloud podem ser adicionados aqui.
 * No CI, a cobertura de integração fica nos testes E2E (Playwright).
 */
describe.skip("UserRepository (Supabase Cloud)", () => {
  it("placeholder para testes de integração locais", () => {
    expect(true).toBe(true);
  });
});
